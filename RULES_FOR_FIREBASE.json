{
    "rules": {
        "leaves": {
            ".read": "auth != null && auth.token.email.endsWith('@navgurukul.org')",
            ".write": "auth != null && auth.token.email.endsWith('@navgurukul.org')"
        },
        "users": {
            ".read": "auth != null && auth.token.email.endsWith('@navgurukul.org')",
            ".write": "auth != null && auth.token.email.endsWith('@navgurukul.org')"
        }
    }
}