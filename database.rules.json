{
    "rules": {
        "users": {
            ".read": "auth != null",
            ".write": "auth != null"
        },
        "leaves": {
            ".read": "auth != null",
            ".write": "auth != null",
            ".indexOn": [
                "userId",
                "status",
                "appliedDate"
            ]
        }
    }
}